import Food from '../../models/food';

class FoodRepository extends  {
  public async findByDate(expiredDate: Date): Promise<Food | null> {

    });

    return findFoodDate || null;
  }
}

export default FoodRepository;
